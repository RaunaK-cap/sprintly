import Image from "next/image";
import { Card } from "@repo/ui/card";
import { Gradient } from "@repo/ui/gradient";
import { TurborepoLogo } from "@repo/ui/turborepo-logo";

export default function Page() {
  return (
    <main className="flex flex-col items-center justify-between min-h-screen p-24 bg-neutral-200">
      <div>
        <div>
          <h2 className=" text-neutral-800 text-sm font-mono"> this is gonna be sprintly landing page ....</h2>
        </div>
      </div>
    </main>
  );
}
